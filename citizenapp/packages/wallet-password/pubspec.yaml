name: gmb_wallet_password
description: 本产品内部的 Substrate BIP-39 password 输入、校验与规范化真源。
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  characters: ^1.3.0
  flutter:
    sdk: flutter
  # 用户确认锁定 ^0.7.0；实际解析版本由各项目 pubspec.lock 固定。
  substrate_bip39: ^0.7.0
  unorm_dart: ^0.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
