#!/usr/bin/env node
import { spawnSync as runExactProcess } from 'node:child_process';

function validateCandidate() {
  const value=process.env;
if(!/^[0-9a-f]{40}$/.test(value.SOURCE_SHA||'')||!/^[1-9][0-9]*$/.test(value.CI_RUN_ID||'')||!/^\d+\.\d{1,2}\.\d{1,2}$/.test(value.SOFTWARE_VERSION||'')||value.VERSION_TAG!=='citizenchain-node-macos-v'+value.SOFTWARE_VERSION)throw Error('准确Release候选无效');
}

// 本文件只执行 gmb.citizenchain-node.macos.release 的 build-desktop Job；阶段编号由本仓唯一 Workflow 固定，禁止接收其它身份。
export const EXACT_REMOTE_JOB_IDENTITY = Object.freeze({"pipeline":"gmb.citizenchain-node.macos.release","job":"build-desktop"});

function requireExactRemoteJobEnvironment() {
  const owner = EXACT_REMOTE_JOB_IDENTITY.pipeline.split('.')[0];
  const expected = { gmb: 'VoyagerRhett/GMB', tuyu: 'VoyagerRhett/TUYU', tata: 'VoyagerRhett/TATA' }[owner];
  if (!expected || process.env.GITHUB_REPOSITORY !== expected) {
    throw new Error('准确远端Job仓库身份无效');
  }
}
const workflowSteps = Object.freeze({"0":{"shell":"bash","source":"set -euo pipefail\ntest \"$(git rev-parse HEAD)\" = \"$GMB_SOURCE_SHA\"\nnode $GITHUB_WORKSPACE/citizenchain/scripts/node/release/macos/index.mjs node-version apply \"$GMB_SOFTWARE_VERSION\"\n"},"1":{"shell":"bash","source":"set -euo pipefail\nif [ \"${RUNNER_ARCH}\" != \"${EXPECTED_RUNNER_ARCH}\" ]; then\n  echo \"::error::runner 架构不符合 ${EXPECTED_RUNNER_ARCH}: 当前为 ${RUNNER_ARCH}\"\n  exit 1\nfi\n"},"2":{"shell":"bash","source":"set -euo pipefail\nnode <<'NODE'\nconst fs = require('fs');\nconst targetSource = process.env.CITIZENCHAIN_MANUAL_BUNDLE_TARGETS;\nconst bundleTargets = (targetSource || '')\n  .split(',')\n  .map((target) => target.trim())\n  .filter(Boolean);\nif (bundleTargets.length === 0) {\n  throw new Error('缺少本 matrix 的 Tauri bundle targets。');\n}\n// 候选版本已在当前 runner 工作区应用；这里只设置本 matrix 的 bundle 目标。\nconst tauriPath = 'citizenchain/node/tauri.conf.json';\nconst tauri = JSON.parse(fs.readFileSync(tauriPath, 'utf8'));\nconst version = tauri.version;\ntauri.bundle = tauri.bundle || {};\n// 中文注释：Release 生成安装包与 updater 正式文件；updater 在同一 runner 内签名。\ntauri.bundle.targets = bundleTargets;\n// 中文注释：按现有平台归档格式在后续步骤签名，禁止再交给本机下载后二次处理。\ndelete tauri.bundle.createUpdaterArtifacts;\n// 中文注释：平台 Release 可以独立成功，因此每个安装包只读取自己的更新清单；\n// 不能再用一个根 version 同时描述版本可能不同的四个目标。\ntauri.plugins = tauri.plugins || {};\ntauri.plugins.updater = tauri.plugins.updater || {};\ntauri.plugins.updater.endpoints = [\n  `https://github.com/VoyagerRhett/GMB/releases/download/${process.env.GMB_VERSION_TAG}/${process.env.CITIZENCHAIN_UPDATER_MANIFEST_NAME}`,\n];\nfs.writeFileSync(tauriPath, `${JSON.stringify(tauri, null, 2)}\\n`);\nconsole.log(`桌面端版本(源码): ${version}`);\nconsole.log(`Tauri bundle targets: ${bundleTargets.join(',')}`);\nNODE\n"},"3":{"shell":"bash","source":"set -euo pipefail\ncase \"$RUNNER_OS/$RUNNER_ARCH\" in\n  macOS/ARM64) platform=macos ;;\n  Windows/X64) platform=windows ;;\n  Linux/ARM64) platform=linux-arm ;;\n  Linux/X64) platform=linux-amd ;;\n  *) echo 'CitizenChain protoc宿主不受支持' >&2; exit 1 ;;\nesac\nprotoc_executable=\"$(node citizenchain/scripts/dependencies.mjs prepare protoc \"$platform\" \"$RUNNER_TEMP/citizenchain-protoc/$platform\")\"\nprintf 'PROTOC=%s\\n' \"$protoc_executable\" >> \"$GITHUB_ENV\"\n"},"4":{"shell":"bash","source":"brew install llvm"},"5":{"shell":"bash","source":"echo \"LLVM_CONFIG_PATH=$(brew --prefix llvm)/bin/llvm-config\" >> \"$GITHUB_ENV\"\necho \"LIBCLANG_PATH=$(brew --prefix llvm)/lib\" >> \"$GITHUB_ENV\"\n"},"6":{"shell":"bash","source":"# 中文注释：现有节点软件只要求完整可验证的 Apple Silicon 应用签名；正式 updater\n# 签名由本 workflow 的独立 Tauri 私钥完成。显式使用 Tauri 官方 ad-hoc 身份，避免 bundler 自动\n# 生成只有资源封套声明、却缺少 CodeResources 的残缺签名。\necho 'APPLE_SIGNING_IDENTITY=-' >> \"$GITHUB_ENV\"\n"},"7":{"shell":"bash","source":"node $GITHUB_WORKSPACE/citizenchain/scripts/node/release/macos/index.mjs node-version lock \"$GMB_SOFTWARE_VERSION\""},"8":{"shell":"bash","source":"npm --prefix citizenchain/node/frontend ci\nnpm --prefix citizenchain/node/frontend run build\n"},"9":{"shell":"bash","source":"# 中文注释：安装包只携带节点软件；首次启动按冻结 chainspec 本地初始化并联网同步。\nrm -rf citizenchain/node/resources/genesis-state\n"},"10":{"shell":"bash","source":"node frontend/node_modules/@tauri-apps/cli/tauri.js build --ci -- --locked --config \"$GITHUB_WORKSPACE/citizenchain/config.toml\""},"11":{"shell":"bash","source":"set -euo pipefail\ndmg_file=\"$(find citizenchain/target/release/bundle/dmg -name '*.dmg' | head -1)\"\nif [ -z \"$dmg_file\" ]; then\n  echo \"::error::未找到 macOS dmg 安装包。\"\n  exit 1\nfi\nmount_dir=\"$(mktemp -d)\"\nextracted_dir=\"citizenchain-macos-app\"\ncleanup() {\n  hdiutil detach \"$mount_dir\" -quiet 2>/dev/null || true\n  rm -rf \"$mount_dir\"\n}\ntrap cleanup EXIT\nhdiutil attach \"$dmg_file\" -nobrowse -readonly -mountpoint \"$mount_dir\" -quiet\napp_bundle=\"$(find \"$mount_dir\" -maxdepth 1 -type d -name '*.app' | head -1)\"\nif [ -z \"$app_bundle\" ]; then\n  echo \"::error::最终 DMG 中没有 macOS App bundle。\"\n  exit 1\nfi\nrm -rf \"$extracted_dir\"\nmkdir -p \"$extracted_dir\"\nditto \"$app_bundle\" \"$extracted_dir/$(basename \"$app_bundle\")\"\napp_bundle=\"$extracted_dir/$(basename \"$app_bundle\")\"\ncodesign --verify --deep --strict \"$app_bundle\"\nentitlements=\"$(codesign -d --entitlements :- \"$app_bundle\" 2>/dev/null)\"\nfor entitlement in \\\n  com.apple.security.device.camera \\\n  com.apple.security.cs.allow-jit \\\n  com.apple.security.cs.allow-unsigned-executable-memory; do\n  # plutil 的 key path 以点号分层，签名 entitlement 的点号必须转义。\n  entitlement_key_path=\"${entitlement//./\\\\.}\"\n  if [ \"$(printf '%s' \"$entitlements\" | plutil -extract \"$entitlement_key_path\" raw -)\" != \"true\" ]; then\n    echo \"::error::macOS App 缺少 $entitlement。\"\n    exit 1\n  fi\ndone\n/usr/libexec/PlistBuddy -c 'Print :NSCameraUsageDescription' \\\n  \"$app_bundle/Contents/Info.plist\" >/dev/null\n"},"12":{"shell":"bash","source":"set -euo pipefail\nmkdir -p citizenchain-release\nversioned_installer=\"${INSTALLER_NAME%.dmg}-v${GMB_SOFTWARE_VERSION}.dmg\"\nversioned_updater=\"${UPDATER_ASSET_NAME%.app.tar.gz}-v${GMB_SOFTWARE_VERSION}.app.tar.gz\"\ndmg_file=\"$(find citizenchain/target/release/bundle/dmg -name '*.dmg' | head -1)\"\nif [ -z \"$dmg_file\" ]; then\n  echo \"::error::未找到 macOS dmg 安装包。\"\n  exit 1\nfi\ncp \"$dmg_file\" \"citizenchain-release/${versioned_installer}\"\napp_bundle=\"$(find citizenchain-macos-app -maxdepth 1 -type d -name '*.app' | head -1)\"\nif [ -z \"$app_bundle\" ]; then\n  echo \"::error::Release 缺少从最终 DMG 验证并提取的 macOS App bundle。\"\n  exit 1\nfi\n# 中文注释：保持 Tauri updater 要求的 .app 根目录归档格式，并在同一 runner 内签名。\nCOPYFILE_DISABLE=1 tar \\\n  -C \"$(dirname \"$app_bundle\")\" \\\n  -czf \"citizenchain-release/${versioned_updater}\" \\\n  \"$(basename \"$app_bundle\")\"\n(cd citizenchain/node && node frontend/node_modules/@tauri-apps/cli/tauri.js signer sign \"$GITHUB_WORKSPACE/citizenchain-release/${versioned_updater}\")\nsignature=\"$(tr -d '\\r\\n' < \"citizenchain-release/${versioned_updater}.sig\")\"\ntest -n \"$signature\"\nnode - \"$versioned_updater\" \"$signature\" <<'NODE'\nconst fs = require('node:fs');\nconst [asset, signature] = process.argv.slice(2);\nfs.writeFileSync('citizenchain-release/citizenchain-node-latest-macOS.json', `${JSON.stringify({\n  version: process.env.GMB_SOFTWARE_VERSION,\n  notes: `CitizenChain macOS v${process.env.GMB_SOFTWARE_VERSION}。`,\n  pub_date: new Date().toISOString(),\n  platforms: { 'darwin-aarch64': {\n    signature,\n    url: `https://github.com/${process.env.GITHUB_REPOSITORY}/releases/download/${process.env.GMB_VERSION_TAG}/${asset}`,\n  } },\n}, null, 2)}\\n`);\nNODE\nls -lh citizenchain-release\n"}});
function runExactWorkflowStep(index) {
  requireExactRemoteJobEnvironment();
  if (!/^(?:0|[1-9][0-9]*)$/.test(String(index || '')) || !Object.hasOwn(workflowSteps, String(index))) {
    throw new Error('准确远端Job阶段无效');
  }
  const step = workflowSteps[String(index)];
  const command = step.shell === 'pwsh' ? 'pwsh' : (process.platform === 'win32' ? 'bash' : '/bin/bash');
  const args = step.shell === 'pwsh'
    ? ['-NoLogo', '-NoProfile', '-NonInteractive', '-Command', step.source]
    : ['--noprofile', '--norc', '-e', '-o', 'pipefail', '-c', step.source];
  const result = runExactProcess(command, args, { cwd: process.cwd(), env: process.env, stdio: 'inherit' });
  if (result.error) throw new Error('准确远端Job阶段无法启动');
  if (result.status !== 0) process.exitCode = Number.isInteger(result.status) ? result.status : 1;
}

requireExactRemoteJobEnvironment();
validateCandidate();
if (process.argv[2] !== 'workflow-step') throw new Error('准确Release Job只接受workflow-step');
runExactWorkflowStep(process.argv[3]);
